version: '3.1'

services:
  db:
    image: postgres
    restart: always
    ports:
      - "54321:5432"
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypass
      POSTGRES_DB: clojurians_log
